1. 论文与所有论文图片放在 `Essay/` 目录下，都使用 Wang 的版本，其他文件夹中的图片一般都在 `Essay/` 中有对应版本，但都需要在 `TaskX/` 目录下保存对应任务的图像。
2. `Chen/` 目录下主要是 Task2 和 Task4 的分析脚本和图像，只需提取其中 Task4 的内容，而 Task2 的内容在本地的 `Task2/` 中版本较新，有更多的修改，保留本地。
3. `Wang/` 目录下有 Task3 的分析脚本和图像，其余大多与本地版本相同，你进行对比并保留。
4. 三个版本中所有出现过的文件必须保留，按照其内容进行分门别类的整理，不能丢失。文件命名要统一，可做适当调整。